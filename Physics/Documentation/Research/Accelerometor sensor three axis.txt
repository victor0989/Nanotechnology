/*Accelerometor sensor three axis
notes
/*Stepper Motor for signal detection in high range scale
detector into the motor? speak with GPT
/* Librerias chat o video



entity ThreeAxis_Accelerometer_is
GENERIC(
   clk_freq    : INTEGER = 50;
   data_rate   : STD_LOGIC_VECTOR := "0100";
   data_range  : STD_LOGIC_VECTOR := "00");

PORT(
  clk        : IN        STD_LOGIC;
  reset_n    :
  mino       : IN
  sclk       : BUFFER
  ss_n       : BUFFER    STD_LOGIC_VECTOR(0 DOWNTO 0);
  moni       : OUT
   lcd_mn
   lcd_rst  

)

end ThreeAxis_Accelerometer;

arquitecture Behavioral of ThreeAxix_Accelerometer is

begin


end Behavioral;